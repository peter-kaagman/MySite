oauth_providers:
  google:
    redirect_uri: "https://mysite.prjv.nl/auth/callback/google"

# OAuth callback URLs voor productie
# plugins:
#   "Auth::OAuth":
#     providers:
#       Google:
#         callback_url: "https://mysite.prjv.nl/auth/google/callback"
#       Github:
#         callback_url: "https://mysite.prjv.nl/auth/github/callback"
# configuration file for production environment

# only log warning and error messages
log: "debug"

# use Log4perl for structured logging
logger: "log4perl"
log4perl:
  config: |
    log4perl.rootLogger = DEBUG, LOGFILE, STDERR
    log4perl.appender.LOGFILE = Log::Log4perl::Appender::File
    log4perl.appender.LOGFILE.filename = logs/mysite.log
    log4perl.appender.LOGFILE.mode = append
    log4perl.appender.LOGFILE.layout = PatternLayout
    log4perl.appender.LOGFILE.layout.ConversionPattern = [%d] [%p] %m%n
    log4perl.appender.STDERR = Log::Log4perl::Appender::Screen
    log4perl.appender.STDERR.stderr = 1
    log4perl.appender.STDERR.layout = PatternLayout
    log4perl.appender.STDERR.layout.ConversionPattern = [%d] [%p] %m%n

# hide errors
show_stacktrace: 0

# disable server tokens in production environments
no_server_tokens: 1
